#include<stdio.h>
#include<conio.h>
#include<string.h>
int main ( )
{
char x[10];
printf("enter a strings to x");
scanf("%s",&x);
printf("\n x string length is =%d",strlen(x));
getch( );
}

